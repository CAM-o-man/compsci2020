from adder import sigma
from digit_counter import counter
from digitavg import averager
from halfcounter import *
from triangle_printer import tri

counter(int(input("Please input a number: ")))
sigma(int(input("Please input a number: ")))
